import { PrototypeScopePage } from "@/components/layout/prototype-scope-page";

export default function SettingsPage() {
  return (
    <PrototypeScopePage
      eyebrow="Prototype scope"
      title="Settings are not needed for this demo."
      description="AstroLive keeps the current experience focused on a clear interpretation and a considered human handoff."
    />
  );
}

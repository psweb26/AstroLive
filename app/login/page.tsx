import { PrototypeScopePage } from "@/components/layout/prototype-scope-page";

export default function LoginPage() {
  return (
    <PrototypeScopePage
      eyebrow="Prototype scope"
      title="Accounts are not part of this prototype."
      description="AstroLive currently focuses on understanding a concern, finding a relevant specialist, and preparing a more focused consultation."
    />
  );
}

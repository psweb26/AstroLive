'use client';
import Link from 'next/link'; import { useEffect,useState } from 'react'; import { loadStoredSessionBrief } from '../../lib/insight-session'; import type { SessionBrief } from '../../src/core/session-brief/buildSessionBrief';
export default function SessionBriefPage(){const [brief,setBrief]=useState<SessionBrief|null|undefined>();useEffect(()=>setBrief(loadStoredSessionBrief(sessionStorage)),[]);if(brief===undefined)return null;if(!brief)return <main className="mx-auto max-w-2xl p-12"><h1 className="text-3xl font-semibold">Session Brief unavailable</h1><Link href="/recommendations">Return to recommendations</Link></main>;return <main className="mx-auto max-w-2xl p-12"><h1 className="text-3xl font-semibold">Your Session Brief</h1><p className="mt-3">{brief.expected_outcome}</p><h2 className="mt-6 font-semibold">Consultation focus</h2><ul>{brief.youll_probably_discuss.map(x=><li key={x}>{x}</li>)}</ul><h2 className="mt-6 font-semibold">Suggested questions</h2><ul>{brief.suggested_questions.map(x=><li key={x}>{x}</li>)}</ul><p className="mt-6">{brief.pro_tip}</p></main>}

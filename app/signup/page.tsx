import { PrototypeScopePage } from "@/components/layout/prototype-scope-page";

export default function SignupPage() {
  return (
    <PrototypeScopePage
      eyebrow="Prototype scope"
      title="Creating an account is not needed to begin."
      description="The complete AstroLive demo flow works within this browser session, without asking for sign-up or payment details."
    />
  );
}
